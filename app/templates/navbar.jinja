<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="nav-link active" aria-current="page" href="{{ url_for('index') }}">Home</a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link" href="{{ url_for('add_pokemon') }}">Add Pokemon</a>
        <a class="nav-link" href="{{ url_for('poke151') }}">Original 151</a>

        {% if current_user.is_active %}
        <a class="nav-link" href="{{ url_for('show_user', username=current_user.username) }}">Profile</a>
        <a class="nav-link" href="{{ url_for('collection') }}">Collection</a>
        <a class="nav-link" href="{{ url_for('logout') }}">Sign Out</a>
        {% else %}
        <a class="nav-link" href="{{ url_for('register') }}">Register</a>
        <a class="nav-link" href="{{ url_for('login') }}">Log In</a>
        {% endif %}
      </div>
    </div>
  </div>
</nav>


